---
import Layout from '~/layouts/page.astro';

import NPMJS from '~/components/content/package/folder-execute.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Công cụ Python chuyển tệp và thư mục (FFT)',
	description: 'File and folder transfer (FFT) là công cụ Python tự động hóa quá trình sao chép hoặc di chuyển tệp và thư mục giữa hai thư mục.',
};
---

<Layout metadata={metadata}>

	<NPMJS/>

	<Action/>

</Layout>