---
import Layout from '~/layouts/page.astro';

import NPMJS from '~/components/content/package/markdown-attribute.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Tiện ích Python sửa thuộc tính markdown (MAM)',
	description: 'Công cụ sửa thuộc tính markdown (Markdown attribute modifier – MAM) là một tiện ích Python tự động hóa việc thay đổi thuộc tính ngày tháng (pubDatetime) và ảnh bìa (image) trong tệp Markdown.',
};
---

<Layout metadata={metadata}>

	<NPMJS/>

	<Action/>

</Layout>