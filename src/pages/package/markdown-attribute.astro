---
import Layout from '~/layouts/page.astro';

import NPMJS from '~/components/content/package/markdown-attribute.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Công cụ Python sửa thuộc tính markdown (MAM)',
	description: 'Markdown attribute modifier (MAM) là công cụ Python sửa thuộc tính markdown (MAM) tự động cập nhật ngày tháng và ảnh bìa trong tệp Markdown, hỗ trợ quản lý nội dung hiệu quả.',
};
---

<Layout metadata={metadata}>

	<NPMJS/>

	<Action/>

</Layout>