---
import Layout from '~/layouts/page.astro';

import NPMJS from '~/components/content/package/npmjs.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Công cụ đo lường lượt tải trình tệp lệnh NPMJS',
	description: 'Công cụ đo lường lượt tải trình tệp lệnh NPMJS được triển khai để thống kê, đo lường lượt tải các gói NPMJS trong thời gian 7 ngày gần nhất.',
};
---

<Layout metadata={metadata}>

	<NPMJS/>

	<Action/>

</Layout>