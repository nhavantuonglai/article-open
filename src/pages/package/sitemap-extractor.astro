---
import Layout from '~/layouts/page.astro';

import NPMJS from '~/components/content/package/sitemap-extractor.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Tiện ích Python trích xuất sitemap và liên kết bên ngoài (SLE)',
	description: 'Công cụ trích xuất sitemap và liên kết bên ngoài (Sitemap and link extractor – SLE) là tiện ích Python dùng để trích xuất URL từ sitemap hoặc từ danh sách URL.',
};
---

<Layout metadata={metadata}>

	<NPMJS/>

	<Action/>

</Layout>