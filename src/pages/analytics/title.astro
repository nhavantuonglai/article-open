---
import Layout from '~/layouts/page.astro';

import Title from '~/components/content/analytics/title.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Công cụ trích xuất tiêu đề từ liên kết',
	description: 'Công cụ trích xuất tiêu đề từ liên kết giúp chuẩn hóa và tối ưu dữ liệu, phục vụ hiệu quả các chiến dịch nghiên cứu, marketing trên mạng xã hội.',
};
---

<Layout metadata={metadata}>

	<Title/>

	<Action/>

</Layout>