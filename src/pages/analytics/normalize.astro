---
import Layout from '~/layouts/page.astro';

import Normalize from '~/components/content/analytics/normalize.astro';

import Action from '~/components/content/main/action.astro';

const metadata = {
	title: 'Công cụ chuẩn hóa dữ liệu thô',
	description: 'Công cụ chuẩn hóa dữ liệu thô giúp làm sạch và lọc văn bản, đảm bảo dữ liệu trở nên dễ sử dụng, tối ưu hóa hiệu quả công việc và phân tích.',
};
---

<Layout metadata={metadata}>

	<Normalize/>

	<Action/>

</Layout>